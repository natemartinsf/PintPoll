# Supabase configuration
# Get these from your Supabase project: Settings > API
PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# Use the "Publishable key" (sb_publishable_xxx), not the legacy anon key
PUBLIC_SUPABASE_ANON_KEY=your-publishable-key-here
# Service role key (secret!) - only used server-side for admin operations
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Resend (email notifications for access requests)
# Sign up at https://resend.com, verify your domain, and create an API key
RESEND_API_KEY=re_your-api-key-here
# Email address to receive access request notifications
NOTIFY_EMAIL=you@example.com
